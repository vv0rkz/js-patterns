# Observer Pattern

Паттерн проектирования, позволяющий объектам (observers) подписываться на события другого объекта (observable) и автоматически получать уведомления об изменениях.

## 🎯 Концепция

```
Observable (Subject)          Observers
    ┌─────────┐              ┌─────────┐
    │ State   │──notify()───▶│Observer1│
    │ +notify │              └─────────┘
    │+subscribe│              ┌─────────┐
    └─────────┘──────────────▶│Observer2│
                              └─────────┘
                              ┌─────────┐
                              │Observer3│
                              └─────────┘
```

**Принцип**: Один источник данных → множество подписчиков → автоматическое обновление при изменениях.

## 📂 Версии

### v1.x — Theme Toggle (Основы)
- **v1.0**: Базовый Observable + Theme Toggle ✅
- **v1.1**: Unsubscribe (отписка от уведомлений)
- **v1.2**: Множественные Observables (тема + язык)

### v2.x — Multi-Timezone Clocks (Продвинутый)
- **v2.0**: Центральный таймер + несколько часов
- **v2.1**: Добавление/удаление часов динамически
- **v2.2**: Pause/Resume для всех часов

### v3.x — Weather Dashboard (Реальное применение)
- **v3.0**: Fetch погоды + базовые виджеты
- **v3.1**: Auto-refresh каждые 30 секунд
- **v3.2**: Уведомления при критических изменениях

## 🔑 Ключевые понятия

### Observable (Наблюдаемый объект)
Объект, который:
- Хранит список подписчиков (`observers: []`)
- Предоставляет метод `subscribe(fn)` для добавления подписчиков
- Предоставляет метод `notify(data)` для уведомления всех подписчиков
- Не знает о деталях реализации подписчиков

### Observer (Наблюдатель)
Функция, которая:
- Регистрируется через `subscribe()`
- Автоматически вызывается при `notify()`
- Получает данные от Observable
- Может быть отписана в любой момент

## 💡 Преимущества

✅ **Separation of Concerns** — Observable не зависит от UI  
✅ **Scalability** — легко добавлять новых подписчиков  
✅ **Reusability** — один Observable для многих компонентов  
✅ **Maintainability** — изменения в одном месте  

## ⚠️ Недостатки

❌ **Memory Leaks** — забытые подписки могут привести к утечкам памяти  
❌ **Debugging** — сложно отследить цепочку вызовов  
❌ **Performance** — слишком много подписчиков → медленные обновления  

## 🚀 С чего начать?

1. Начни с [v1-theme-toggle](v1-theme-toggle/) — базовый пример
2. Реализуй код по комментариям в файлах
3. Тестируй в браузере
4. Переходи к следующей версии

## 📚 Ресурсы

- [Observer Pattern - Patterns.dev](https://www.patterns.dev/posts/observer-pattern/)
- [Observer Pattern - Refactoring Guru](https://refactoring.guru/design-patterns/observer)
- [RxJS - Reactive Programming](https://rxjs.dev/)

---

**Practice Mode активирован** — учись делая, а не копируя! 💪
